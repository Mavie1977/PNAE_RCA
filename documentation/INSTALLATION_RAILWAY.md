# Railway

Le dépôt doit contenir : artisan, composer.json, app, bootstrap, config, database, public, resources, routes, storage.

Commandes Railway :
```bash
php artisan migrate:fresh --seed --force
php artisan optimize:clear
```
